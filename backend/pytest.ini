[pytest]
pythonpath = .
asyncio_mode = auto
testpaths = tests
markers =
    slow: exercises a real ML model (sentence-transformers); skip with -m "not slow"
filterwarnings =
    ignore::DeprecationWarning
